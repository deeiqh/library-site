extends layout

block content 

    h1 Author List

    ul
        each author in data
            li 
                a(href=author.url) #{author.name}
                |  (#{author.lifespan})
        else
            li There are no authors.