extends layout 

block content 
    h1 #{title}

    form(method='post' action='')
        label(for='name') Name: 
        input(type='text' id='name' name='name' value=genre ? genre.name : '')
        button(type='submit') Submit
        if errors
            ul 
                for error in errors
                    li #{error.msg}